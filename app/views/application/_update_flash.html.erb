<% flash.each do |key, msg| %>
    <%= content_tag :div, msg, :class => [key, " message"], :id => "notice_#{key}" %>
    <%= content_tag :script, :type => "text/javascript" do -%>
        setTimeout(function() {
			$('div#notice_<%= key %>').slideUp(400, function() {$(this).remove();});
			}, 15000);
    <% end %>
	<% flash.discard %>
<% end %>

<%= content_tag :script, :type => "text/javascript" do %>
	$(document).ready( function() {
		$('.message').slideDown();
	});
<% end %>